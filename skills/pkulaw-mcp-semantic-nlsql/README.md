# pkulaw-mcp-semantic-nlsql

法宝语义检索（NL-SQL）Skill（`semantic-nlsql`）。基于北大法宝原生 MCP 服务能力，用于跨库综合语义检索；如需终端配置、复现与排障，请使用 npm CLI 包 `@pkulaw/mcp-cli`，安装后命令为 `pkulaw-mcp`，使用前需完成实例路径配置。

- 主文件：[SKILL.md](./SKILL.md)
